apiVersion: v1
kind: Service
metadata:
  name: demo-service
  namespace: demo-namespace
  labels:
    app: demo
spec:
  type: ClusterIP # safest default, no external exposure
  selector:
    app: demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
